network:
  version: 2
  renderer: networkd
  ethernets:
    {{ INTERFACE_NAME }}:
      addresses:
        - {{ variables.network.TARGET_IP_ADDRESS }}
      nameservers:
        addresses: [{{ variables.network.DNS_SERVER }}]
      routes:
        - to: default
          via: {{ variables.network.DEFAULT_GATEWAY  }}
